<?php
declare(strict_types=1);require_once __DIR__.'/includes/bootstrap.php';if(current_user()){header('Location: dashboard.php');exit;}$error='';if($_SERVER['REQUEST_METHOD']==='POST'){verify_csrf($_POST['csrf']??null);$identity=trim($_POST['identity']??'');$password=(string)($_POST['password']??'');$lookup=filter_var($identity,FILTER_VALIDATE_EMAIL)?normalize_email($identity):$identity;$q=db()->prepare('SELECT id,password_hash FROM users WHERE (email=? OR username=?) AND is_active=1 LIMIT 1');$q->execute([$lookup,$lookup]);$row=$q->fetch();if($row&&password_verify($password,$row['password_hash'])){session_regenerate_id(true);$_SESSION['user_id']=(int)$row['id'];header('Location: dashboard.php');exit;}usleep(250000);$error='Anmeldedaten nicht korrekt.';}$pageTitle='Anmelden';require __DIR__.'/includes/header.php';?>
<section class="panel auth-wrap"><h1>Anmelden</h1><p class="subtle">Melde dich mit deinem Just-InCard-Konto an.</p><?php if($error):?><div class="alert alert-error"><?=e($error)?></div><?php endif;?><form method="post"><input type="hidden" name="csrf" value="<?=e(csrf_token())?>"><div class="form-row"><label>Benutzername oder E-Mail</label><input class="input" name="identity" required autocomplete="username"></div><div class="form-row"><label>Passwort</label><div class="password-field"><input class="input" type="password" id="login_password" name="password" required autocomplete="current-password"><button class="btn" type="button" data-password-toggle="login_password">anzeigen</button></div></div><button class="btn btn-primary" type="submit" style="width:100%;margin-top:10px">Anmelden</button></form></section>
<?php require __DIR__.'/includes/footer.php';?>
